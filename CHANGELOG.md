# 更新记录

## v1.1.0-test.5 · 2026-09-23（本地测试，未发布）

- 修复关灯成功后立即读到旧的开灯状态：布尔操作完成后核对目标值，首次读回不一致时仅复查刚操作的属性，最多补查 4 次；匹配即结束。
- 确认期间不发布旧状态；同一属性的多个快捷栏绑定一起更新。达到次数或时间上限仍未确认时显示未知并记录 `STATE_UNCONFIRMED`，不重发控制。
- “设置 → 运行状态与日志”新增“复制全部日志”，汇总版本、时间、各类诊断日志及上一段保留日志；自动复制受限时提供手动复制文本框。
- 米家诊断增加目标状态、各次读回值和累计耗时。日志导出限定固定诊断文件，不包含登录凭据或设备缓存。

## v1.1.0-test.4 · 2026-09-23（本地测试，未发布）

- 修复米家操作成功后快捷栏仍显示“状态未知”：每次操作完成后重新读取可显示的设备状态，更新开关图标；空闲时不请求云端。
- 快捷栏直接通过本地 IPC 提交到常驻米家服务，省去每次点击启动 Shell 和 Java 命令进程；操作结果就绪后及时回传界面。
- 布尔属性操作后的状态查询合并为一次批量读回，同一属性的多个绑定共用读取；执行期间防止重复点击。
- 设置页日志增加米家快捷栏通信、排队、指令发送、状态读回及总耗时，日志有大小上限并排除登录凭据。

## v1.1.0-test.3 · 2026-09-23（本地测试，未发布）

- 点击快捷栏中的开关后保持面板打开，可连续执行其他开关；点击应用后仍收起面板并尝试以 ColorOS 小窗打开。
- 米家开关点击后将本次显示状态标为未知，重新展开时再读取设备状态，不增加展开期间的云端查询。
- WebUI 快捷栏预览同步开关点击行为；测试包与菜单组件版本升级为 `103`。

## v1.1.0-test.2 · 2026-09-23（本地测试，未发布）

- 快捷栏每次展开时读取一次已绑定米家设备的可读布尔开关属性；开显示绿色电源按钮，关显示灰色电源按钮。
- 读取中、失败或设备离线分别保持待定/未知状态，不将未知误画成关闭；场景和非布尔动作继续显示执行符号。
- 同一会话只创建一次云端读取任务；重复查询仅获取本地任务结果，下一次展开才重新读取。

## v1.1.0-test.1 · 2026-09-22（本地测试，未发布）

- WebUI 内置墨白极简和石墨工具箱，在快捷栏和设置之间增加米家页。
- 接入真实扫码登录、家庭与设备列表、MIOT 属性控制、设备动作与手动场景。
- 米家操作可绑定到短按、双击、长按或快捷栏，名称自动生成，配置仅保存动作编号。
- 米家服务独立运行，凭据私有保存，网络任务有超时和队列边界；区分受理、状态确认和结果未知，不自动重试控制请求。
- 快捷菜单现有外观和 ColorOS 小窗路径保持不变。只构建本地测试包，尚未完成目标手机端到端实测。

## v1.0.0 · 2026-09-21

首个正式版。

- 短按、双击和长按独立配置，支持识别时间调节和触发震动。
- 自定义侧边快捷菜单，批量勾选应用并自动读取名称与图标。
- 双列滚动布局，透明背景、系统深浅色适配、隐藏未配置项。
- 快捷栏宽度 120～360 dp、应用间距 0～32 dp，支持左右方向和高度设置。
- 菜单应用使用 ColorOS 小窗，支持系统手电筒、媒体控制和自定义动作。
- 精简界面与安装提示，维护工具收进运行状态与日志。
- 统一正式版版本信息、安装包命名和使用说明，支持保留配置覆盖升级。

开发阶段的版本记录见 [Releases](https://github.com/zhujiu39/findx8s-sidekey/releases)。
