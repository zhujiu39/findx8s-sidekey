# v1.1.0-test.4 本地测试记录

日期：2026-09-23。构建入口：`python build.py`。本次仅生成测试包，不推送或发布。

## 测试范围

- 新增独立米家服务、扫码登录、家庭/设备/场景读取、MIOT 属性与动作、手势/快捷栏绑定。
- 墨白极简与石墨工具箱两套 WebUI；导航为手势、快捷栏、米家、设置。
- 保留原快捷菜单外观、应用勾选、宽度和间距设置、ColorOS 小窗、手电筒及震动实现。
- 快捷栏展开时读取一次米家可读布尔属性，分别显示开、关、读取中和未知状态；场景仍显示执行符号。
- 点击快捷开关后菜单保持打开；点击应用后收起并沿用 ColorOS 小窗启动路径。每次米家操作完成后批量读回并显示实际开关状态，空闲时不请求云端。
- 版本 v1.1.0-test.4 / versionCode 104，沿用原菜单 APK 签名，覆盖安装保留已有侧键配置。

## 验证项

- Java 米家测试：RFC 6229 RC4-drop1024 向量、SHA-256 签名随机数、加密字段、登录前缀解析、域名/HTTPS 边界、规格缺少中文翻译、数据类型/范围/步长验证、凭据脱敏、异步任务、只发送一次、网关受理与读回确认的区别、离线时停止切换、账号隔离、绑定持久化、扫码取消后迟到响应不恢复凭据、退出清除及异常重启记录。
- 快捷栏合成设备测试：同一展开只读一次云端状态，重复绑定合并读取，重新展开取新状态，离线显示未知；协议长度和无效状态字符检查。
- 米家共 70 项断言，新增操作完成后的开/关刷新、异步提交、重复提交去重、场景后的设备状态刷新、丢失写入响应后的实际状态读回、网关仅受理、读回失败、服务重启后不重发控制和日志脱敏。布尔切换只保留一次前读和一次后读；空闲本地查询不增加云端请求。
- `python build_mijia.py --live` 已通过真实服务的匿名扫码握手、二维码图片读取和公开设备规格解析，没有登录任何账号，也未保存实际二维码或凭据。
- Node 测试增加米家属性、状态和绑定参数验证；全量结果见构建日志。
- C 配置往返包含米家动作编号；原手势、菜单协议、应用目录、手电筒、ColorOS 小窗和布局测试继续运行。
- 浏览器交互使用隔离的合成家庭测试接口，检查设备详情、执行结果、绑定加入草稿、保存配置、主题显示、333 px 窄屏无横向溢出以及扫码取消按钮。合成数据不随安装包分发。
- Android API 35 编译、ARM64 静态 ELF 与 16 KiB 对齐、独立米家 DEX、菜单 APK 签名、Shell/JavaScript 语法、ZIP 内容和 Unix 权限校验。

## 尚需用户实测

本机没有连接目标手机，也没有你的米家授权。以上结果不能证明手机上的账号登录与设备执行已端到端通过。

1. 覆盖安装测试 ZIP，重启，确认旧快捷栏与小窗功能正常。
2. 米家页扫码授权、选择家庭，核对设备和手动场景。
3. 先执行一个能观察到结果的开关或场景，查看“最近执行结果”。
4. 添加到快捷栏或手势，保存后按侧键测试。
5. 断网时应显示超时/失败或结果未知，控制请求不应被自动重发。
6. 将可读写布尔开关绑定到快捷栏，开时看绿色、关时看灰色；点击后先显示执行中，完成读回后自动显示实际开/关状态，无需收起再展开。断网或离线导致读取失败时应看到未知状态。
7. 同一快捷栏中连续点击两个快捷开关，确认每次执行后面板仍可操作；再点击应用，确认面板收起且应用以小窗打开。
8. 查看设置页日志中的“米家快捷栏通信”和“米家执行与状态读回”，反馈慢操作时一并提供对应阶段耗时。

需要反馈时提供当前测试版本、操作步骤和米家页错误文字即可；不要提供 auth.json、登录令牌或二维码。
