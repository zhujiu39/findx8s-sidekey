# 项目交付约定

- 按用户 2026-09-23 的要求，本项目改动后直接编译打包，由用户实机测试；除非用户明确要求，不运行自动测试、浏览器测试或联网测试。
- 使用 `python build.py` 构建安装包，按当前版本生成 `test_` 或 `release_` 包。默认不运行测试，保留真实编译、签名和安装包完整性检查。
- 未获新的发布指令前，不执行 Git push、创建远程标签或发布 Release。
- 每次交付新建目录，保留既有配置兼容性和 APK 签名；私钥、登录凭据和其他项目不得进入提交或交付包。
